package com.addamsestates.image.repo;

import com.addamsestates.image.model.EventsImages;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EventsImagesRepository extends JpaRepository<EventsImages,Long> {
}
